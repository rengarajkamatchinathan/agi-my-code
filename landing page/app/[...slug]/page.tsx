import { ArrowLeft, ArrowRight, ArrowUpRight, Check, Copy, Terminal } from 'lucide-react'
import { Reveal, Spotlight, Shimmer } from '@/components/reactbits-motion'

const github = 'https://github.com/your-username/my-code'
const docs = [['Quickstart','/developers/quickstart'],['CLI reference','/developers/cli'],['SDK','/developers/sdk'],['MCP','/developers/mcp'],['Plugins','/developers/plugins'],['Architecture','/developers/architecture']]
const featureGroups = [['AUTONOMOUS CODING','Understand intent, inspect context, make changes and verify the result.'],['SHELL EXECUTION','Run commands, tests, builds and background processes with explicit permissions.'],['CODEBASE INTELLIGENCE','Search, LSP references, diagnostics and relevant-file discovery in one loop.'],['WEB & RESEARCH','Bring current documentation and external context into a controlled session.'],['CONTEXT MANAGEMENT','SNIP, MICROCOMPACT, CONTEXT COLLAPSE and AUTOCOMPACT keep sessions useful.'],['PLANNING','Turn ambiguous work into a visible plan before changing code.'],['GIT WORKTREES','Isolate parallel work without losing your current branch.'],['SESSIONS','Resume focused work across terminals and environments.'],['MULTI-AGENT SWARMS','Delegate research, coding, testing and review to focused agents.'],['PERSISTENT MEMORY','Carry project decisions and patterns between sessions.'],['SCHEDULED TASKS','Run repeatable agent work on a schedule.'],['IDE BRIDGE','Forward context, diagnostics and permissions between terminal and IDE.'],['VOICE INPUT','Submit intent without leaving the workflow.']]
const pageData: Record<string,{eyebrow:string;title:string;intro:string}> = {
  developers:{eyebrow:'DEVELOPERS / START HERE',title:'Build with the agent that builds with you.',intro:'Install the CLI, compose the SDK, or connect external tools through MCP. The docs are designed to get out of your way.'},
  download:{eyebrow:'DOWNLOAD / OPEN SOURCE',title:'Put an agent in your terminal.',intro:'Choose your platform and package manager. Bring your own model and keep your workflow.'},
  community:{eyebrow:'COMMUNITY / OPEN BY DESIGN',title:'Build the future of developer tools in public.',intro:'My-Code is shaped by people who care about capable software and developer control.'},
  resources:{eyebrow:'RESOURCES / FIELD NOTES',title:'The practical side of agentic development.',intro:'Guides, release notes, and ideas for building software with more context and less ceremony.'},
}
function Header(){return <header className="nav"><a href="/" className="brand"><span className="brand-mark">&gt;_</span> MY-CODE</a><nav className="nav-links"><a href="/features">Product</a><a href="/developers">Developers</a><a href="/resources">Resources</a><a href="/community">Community</a></nav><div className="nav-actions"><a className="nav-docs" href="/developers/quickstart">Docs</a><a className="nav-download" href="/download">Download <ArrowUpRight size={14}/></a></div></header>}
function CopyBlock({children}:{children:string}){return <div className="copy-block"><code>{children}</code><button aria-label="Copy code"><Copy size={14}/> copy</button></div>}
function DocsAside(){return <aside className="docs-aside"><a href="/" className="brand"><span className="brand-mark">&gt;_</span> MY-CODE</a><p className="aside-label">DEVELOPERS</p>{docs.map(([name,href])=><a className="aside-link" href={href} key={href}>{name}</a>)}<a className="aside-source" href={github}>View source <ArrowUpRight size={13}/></a></aside>}
function DocsPage({kind}:{kind:string}){const configs:Record<string,{title:string;intro:string;code:string;items:string[]}>={quickstart:{title:'Quickstart',intro:'From a clean terminal to your first verified change.',code:'npm install -g my-code\nmy-code',items:['Install the CLI globally','Choose a local or cloud model','Ask for a change in plain language','Review the plan, diff and test result']},cli:{title:'CLI reference',intro:'The commands that keep agentic work close to your keyboard.',code:'my-code [prompt]\nmy-code --model qwen2.5-coder\nmy-code --plan\nmy-code --resume <session>',items:['Interactive prompts and streaming output','Model, permission and workspace flags','Sessions, worktrees and command palette','Vim mode and shell completion']},sdk:{title:'SDK',intro:'Embed the same agent loop in your own TypeScript application.',code:'import { createAgent } from "my-code"\n\nconst agent = createAgent({ model, tools })\nawait agent.run("Review this change")',items:['Compose an agent with tools','Stream structured events','Scope permissions per run','Bring your own provider adapter']},mcp:{title:'MCP',intro:'Connect external tools and resources without forking the core.',code:'{\n  "mcpServers": {\n    "docs": { "command": "npx", "args": ["my-docs-mcp"] }\n  }\n}',items:['Declare servers in mcp.json','Expose resources and actions','Review tool calls before execution','Keep credentials outside project files']},plugins:{title:'Plugins',intro:'Shape My-Code around the way your team actually works.',code:'export default {\n  name: "reviewer",\n  commands: { review: async (ctx) => {} },\n  hooks: { afterEdit: async (ctx) => {} }\n}',items:['Add commands and custom tools','Create reusable skills','Hook into agent lifecycle events','Share extensions with your team']},architecture:{title:'Architecture',intro:'A technical map of the layers behind the terminal experience.',code:'ENTRY → BOOTSTRAP → CORE ENGINE\n        ↓\nTOOLS → SERVICES → UI → STATE',items:['Entry: CLI parsing and routing','Bootstrap: runtime state and security gates','Core engine: agentic loop','Tools and services: capabilities and integrations','UI and state: renderer and observable application state']}};const c=configs[kind]||configs.quickstart;return <><Header/><div className="docs-layout"><DocsAside/><article className="article"><a className="back-link" href="/developers"><ArrowLeft size={14}/> Developers</a><p className="section-kicker">DEVELOPERS / {kind.toUpperCase()}</p><h1>{c.title}</h1><p className="article-intro">{c.intro}</p><CopyBlock>{c.code}</CopyBlock><div className="article-sections">{c.items.map((x,i)=><section key={x}><span className="section-index">0{i+1}</span><div><h2>{x}</h2><p>{i===0?'A small, composable surface that stays close to the terminal.':'Keep the agent useful, inspectable and under developer control.'}</p></div></section>)}</div><div className="placeholder"><Terminal size={18}/><span>Examples are intentionally small. Read the source to go deeper.</span><a href={github}>View repository <ArrowUpRight size={13}/></a></div></article></div></>}
function FeaturePage(){return <><Header/><main className="subpage-wrap feature-page"><p className="section-kicker">PRODUCT / CAPABILITIES</p><h1>An agent system,<br/><Shimmer><em>not a chat box.</em></Shimmer></h1><p className="article-intro">Autonomous coding, tools, memory, permissions and sessions in one terminal-native workflow.</p><Reveal><div className="feature-list">{featureGroups.map(([a,b],i)=><Spotlight className="feature-list-item" key={a}><section><span>0{String(i+1).padStart(2,'0')}</span><div><h2>{a}</h2><p>{b}</p></div><ArrowUpRight size={18}/></section></Spotlight>)}</div></Reveal></main></>}
function DownloadPage(){return <><Header/><main className="subpage-wrap download-page"><p className="section-kicker">DOWNLOAD / OPEN SOURCE</p><h1>Choose your<br/><em>starting point.</em></h1><p className="article-intro">My-Code runs on macOS, Linux and Windows. Start with a package manager or build from source.</p><div className="download-options"><section><b>npm</b><p>Recommended for Node.js environments.</p><CopyBlock>npm install -g my-code</CopyBlock></section><section><b>Homebrew</b><p>Install and update with your existing toolchain.</p><CopyBlock>brew install my-code/tap/my-code</CopyBlock></section><section><b>Source</b><p>Inspect, extend and build the open-source repository.</p><a className="button secondary" href={github}>Open GitHub <ArrowUpRight size={15}/></a></section></div><div className="platforms large"><span>macOS</span><span>Linux</span><span>Windows</span></div></main></>}
function ResourceOrCommunity({root,sub}:{root:string;sub:string}){const content=root==='community'?{contributors:['Contribution guide','Good first issues','Plugin ecosystem','Discussions'],roadmap:['Exploring','In progress','Shipped']}:{docs:['Concepts and configuration','CLI reference','SDK examples','MCP guide'],changelog:['v0.4 · Context compression','v0.3 · Multi-agent sessions','v0.2 · MCP support'],blog:['Why terminal-native matters','Designing safe agent loops','Memory without noise']};const items=(content as Record<string,string[]>)[sub]||[];return <><Header/><main className="subpage-wrap resource-page"><p className="section-kicker">{root.toUpperCase()} / {sub.toUpperCase()}</p><h1>{sub==='roadmap'?'A roadmap in public.':sub==='contributors'?'Make the agent better.':`${sub.charAt(0).toUpperCase()+sub.slice(1)} for the terminal.`}</h1><p className="article-intro">{root==='community'?'No invented counts, no closed-door promises. Follow the work and contribute where it matters.':'Practical material for understanding, using and extending My-Code.'}</p><div className="resource-list">{items.map((x,i)=><section key={x}><span>0{i+1}</span><div><h2>{x}</h2><p>{root==='community'?'Open for discussion in the public repository.':'Read the guide, inspect the example, and keep moving.'}</p></div><ArrowRight size={16}/></section>)}</div><div className="placeholder"><Terminal size={18}/><span>Live content will be published here as the project evolves.</span><a href={github}>Follow on GitHub <ArrowUpRight size={13}/></a></div></main></>}
export default async function RoutePage({params}:{params:Promise<{slug:string[]}>}){const {slug}=await params;const [root,sub]=slug; if(root==='features')return <FeaturePage/>;if(root==='download')return <DownloadPage/>;if(root==='developers'&&sub)return <DocsPage kind={sub}/>;if(root==='developers')return <><Header/><main className="subpage-wrap"><p className="section-kicker">DEVELOPERS / START HERE</p><h1>Build with the agent<br/><em>that builds with you.</em></h1><p className="article-intro">Install the CLI, compose the SDK, or connect external tools through MCP.</p><div className="feature-list">{docs.slice(0,5).map(([name,href],i)=><section key={name}><span>0{i+1}</span><div><h2>{name}</h2><p>Focused documentation for building with My-Code.</p></div><a href={href}><ArrowUpRight size={18}/></a></section>)}</div></main></>;if(root==='community'||root==='resources')return sub?<ResourceOrCommunity root={root} sub={sub}/>:<ResourceOrCommunity root={root} sub={root==='community'?'contributors':'docs'}/>;return <FeaturePage/>}
